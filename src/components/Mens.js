import React from 'react'
import { shareData } from '../context/Createcontent';
import { useContext } from 'react';

export const Mens = () => {
    const {}=useContext(shareData)
  return (
    <div>Mens</div>
  )
}
